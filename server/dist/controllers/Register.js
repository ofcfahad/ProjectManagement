"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.notVerified = exports.updateEmail = exports.verifyOTP = exports.sendOTP = exports.createNewUser = void 0;
const User_1 = __importDefault(require("../database/Schemas/User"));
const bcryptjs_1 = __importDefault(require("bcryptjs"));
const nodemailer_1 = __importDefault(require("nodemailer"));
const jsonwebtoken_1 = __importDefault(require("jsonwebtoken"));
const randomstring_1 = __importDefault(require("randomstring"));
const createNewUser = (req, res) => __awaiter(void 0, void 0, void 0, function* () {
    try {
        const { userName, fullName, userPassword, userEmail, userProfilePicture, userGithubLink } = req.body;
        const existingUser = yield User_1.default.findOne({ userName: userName });
        if (existingUser) {
            return res.status(409).json({ message: 'oops! Username is already taken' });
        }
        const hashedPassword = yield bcryptjs_1.default.hash(userPassword, 10);
        const newUser = new User_1.default({ userName: userName, fullName: fullName, userPassword: hashedPassword, userEmail: userEmail, userProfilePicture: userProfilePicture, userGithubLink: userGithubLink, preferences: { theme: 'light', toolTipisVisible: true } });
        yield newUser.save();
        return res.status(201).json({ message: 'Well Well you got it!' });
    }
    catch (error) {
        console.error(`from createNewUser: ${error}`);
        return res.status(500).json({ message: 'Internal server error' });
    }
});
exports.createNewUser = createNewUser;
const savedOtps = {};
const sendOTP = (req, res) => __awaiter(void 0, void 0, void 0, function* () {
    const { userEmail } = req.body;
    const existingUser = yield User_1.default.findOne({ userEmail: userEmail });
    if (existingUser) {
        return res.status(409).json({ message: 'it seems the Email you provided is already bieng used, please try another address' });
    }
    const otp = randomstring_1.default.generate({ length: 6, charset: 'numeric' });
    const otpExpirationTime = Date.now() + 1 * 60 * 1000;
    const { EMAIL, EMAIL_PASS } = process.env;
    savedOtps[userEmail] = {
        otp,
        otpExpirationTime,
    };
    // Configure Nodemailer transport
    const transporter = nodemailer_1.default.createTransport({
        service: 'gmail',
        auth: {
            user: EMAIL,
            pass: EMAIL_PASS,
        },
    });
    const mailOptions = {
        from: 'Project Management',
        to: userEmail,
        subject: 'OTP for email authentication',
        html: `Dear Recipient,
<br />
  We noticed that you requested a One-Time Password (OTP) code to access our system. 
<br /> 
  Here is your OTP <strong id="otp"> ${otp} </strong> 
<br /> 
  We want to remind you that for security reasons, OTP codes are valid only for <strong>${Number((otpExpirationTime - Date.now() + 1) / 1000)}</strong> seconds. After the expiration time, your OTP code will no longer be valid, and you will need to request a new one to access our system. 
<br /> 
  If you have not requested an OTP code, please ignore this email and contact our support team immediately. 
<br /> 
  Thank you for choosing our service. We value your security, and we want to ensure that your information is always protected. 
<br /> 
  Best regards,
<br />
 <strong> PJM </strong>
 <br />
 Generated by <strong> ChatGPT </strong>
 <script>
 var otp = document.getElementById('otp');
 
 otp.onclick = function() {
   navigator.clipboard.writeText(otp.innerText)
     .then(() => {
       alert('OTP copied to clipboard!');
     })
     .catch((error) => {
       console.error('Failed to copy OTP: ', error);
     });
 };
 </script>`,
    };
    transporter.sendMail(mailOptions, (error, info) => {
        if (error) {
            console.log(error);
            res.status(500).send('Error sending OTP');
        }
        else {
            console.log('Email sent: ' + info.response);
            res.status(200).send('Email Sent');
        }
    });
});
exports.sendOTP = sendOTP;
const verifyOTP = (req, res) => {
    const { userEmail, otp } = req.body;
    if (!savedOtps || savedOtps[userEmail].otp !== otp || Number(savedOtps[userEmail].otpExpirationTime) < Date.now()) {
        res.status(400).send('Invalid or Expired OTP');
    }
    else {
        res.status(200).send('OTP verified');
    }
};
exports.verifyOTP = verifyOTP;
const updateEmail = (req, res) => __awaiter(void 0, void 0, void 0, function* () {
    const { userName, userPassword, userEmail } = req.body;
    try {
        const user = yield User_1.default.findOne({ userName });
        const userId = user === null || user === void 0 ? void 0 : user._id;
        if (user && (yield bcryptjs_1.default.compare(userPassword, user.userPassword))) {
            yield User_1.default.findByIdAndUpdate(userId, { userEmail: userEmail, verified: true });
            const secret = process.env.SECRET_KEY;
            const token = jsonwebtoken_1.default.sign({ userId }, secret, { expiresIn: '7d' });
            res.status(200).json({ message: 'welcome aboard', token });
        }
        else {
            res.status(500).send('well guess something is gone wrong');
        }
    }
    catch (error) {
        console.log(`from updateEmail: ${error}`);
    }
});
exports.updateEmail = updateEmail;
const notVerified = (req, res) => __awaiter(void 0, void 0, void 0, function* () {
    const { userName, userPassword } = req.body;
    try {
        const user = yield User_1.default.findOne({ userName });
        const userId = user === null || user === void 0 ? void 0 : user._id;
        if (user && (yield bcryptjs_1.default.compare(userPassword, user.userPassword))) {
            yield User_1.default.findByIdAndUpdate(userId, { verified: false });
            const secret = process.env.SECRET_KEY;
            const token = jsonwebtoken_1.default.sign({ userId }, secret, { expiresIn: '7d' });
            res.status(200).json({ message: 'welcome aboard', token });
        }
        else {
            res.status(500).send('well guess something is gone wrong');
        }
    }
    catch (error) {
        console.log(`from notVerified: ${error}`);
    }
});
exports.notVerified = notVerified;
//# sourceMappingURL=Register.js.map